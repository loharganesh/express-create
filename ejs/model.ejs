const mongoose = require('mongoose');

const schema = mongoose.Schema({ 
    
    // Write your model attributes here

});

const <%-modelName%> = mongoose.model('<%-modelName%>',schema);
module.exports = <%-modelName%>